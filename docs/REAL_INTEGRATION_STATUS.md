# Real Integration Status

| service | code exists | authenticated | real external request succeeded | proof | blocker |
| Hermes | no (the repository `src/hermes.ts` is local orchestration, not the Hermes Agent runtime) | no (the AWS runtime has no usable authenticated model credential yet) | no | On AWS instance `i-0ef289591a906638b`, official NousResearch installer completed through SSM Run Command `b3ff55aa-14e1-4954-9ec8-e76f2cb880c7`; `/usr/local/bin/hermes` reports Hermes Agent v0.21.2 (2026.9.11), upstream `b9271bcb`; AWS-side `hermes status` completed successfully through SSM command `fcf2f922-099c-479c-91a7-8a0ba0ada62e` and reports provider AWS Bedrock, model `anthropic/claude-opus-4.6`, gateway stopped, Google/Gemini not configured, and Slack not configured | Gemini/provider credentials are missing; gateway is not running |
| Gemini | yes (live REST adapter) | no | no | `src/gemini.ts` contains the live `generateContent` adapter; no live request has succeeded | `GEMINI_API_KEY` is missing |
| Slack | yes (live post and callback server) | no | no | `src/slack.ts` contains the live `chat.postMessage` adapter and signed callback server; only mock payload/parser tests have run | bot token, signing secret, target channel, and reachable callback URL are not configured |
| Gmail | yes (draft/live adapter) | no | no | `src/gmail.ts` contains Gmail draft/send adapters; only dry-run and idempotency tests have run | Google OAuth credentials are not configured |
| Google Sheets | yes (live append adapter) | no | no | `src/sheets.ts` contains the Sheets values append adapter; only mock append tests have run | Google OAuth credentials and target spreadsheet are not configured |
| Greenhouse | yes (live public-board adapter) | n/a (public board) | yes | GuidePoint Security board returned HTTP 200 with 56 jobs; a real job ID, title, and source URL were observed | no authentication blocker; fallback fixture still needs to be saved |
| AWS | no | yes | yes | AWS account `741928001229`, region `us-east-1`; exactly one running Odyva EC2 instance `i-0ef289591a906638b`; budget `odyva-hackathon-budget` configured with a $20 monthly limit and $10/$16/$18/$20 actual-cost notifications to `aditya@motionviz.in`; SSM agent online and SSM Run Command succeeded on the active instance; no unattached Odyva volume or Elastic IP remains | Slack Socket Mode avoids a public HTTPS callback; Gemini, Slack, Gmail, and Sheets credentials remain to be configured |
